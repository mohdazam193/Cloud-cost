module.exports = {
  schema: {
    userId: 'string',
    totalMonthlyCost: 'number',
    underutilizedEC2: 'array',
    underutilizedEBS: 'array',
    timestamp: 'date'
  }
};


// Models are the schema of database 
